use dl_network_common::Connection;

/// Handles login and signup attempts from the client
/// returns true if disconnecting
pub fn login_handler(connection: &mut Connection) -> bool {
    // todo(skepz)
    false
}